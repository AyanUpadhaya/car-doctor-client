import Carousel from "../Components/Carousel";

const Home = () => {
    return (
        <div className="container mx-auto">
            <Carousel></Carousel>
        </div>
    );
};

export default Home;